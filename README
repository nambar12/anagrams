Anagrams Matcher

Your task is to create a program which finds anagrams within a given input. Two strings are considered anagrams
if they have the same letters with different permutations. For example: "abbcdef" abd "fedcbab" are anagrams.
They should be of the same length and each letter should show up
